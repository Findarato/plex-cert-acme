#!/bin/sh
# Uncomment the one you are using below Could easily be used with others, but I am only doing cloudflare and the origional

#export AWS_ACCESS_KEY_ID=<YOUR AWS ACCESS KEY ID>
#export AWS_SECRET_ACCESS_KEY=<YOUR AWS AWS_SECRET_ACCESS_KEYD>
#/home/plex/.acme.sh/acme.sh --force --issue --dns dns_aws -d <YOUR PLEX HOST DNS NAME> --reloadcmd "/home/plex/reload-cert.sh"


#export CF_Token=<YOUR CLOUDFLAIRE TOKEN>
#/home/plex/.acme.sh/acme.sh --server letsencrypt --force --issue -d <YOUR PLEX HOST DNS NAME> --dns dns_cf  --reloadcmd "/home/plex/plexCert/reload-cert.sh"